-r ./base.txt

psycopg2==2.7.7            # postgresql interface
astroid==2.1.0             # for pylint
colorama==0.4.1            # only for Windows, enables colored console output
isort==4.3.4               # sorts import statements in python code, only for development
lazy-object-proxy==1.3.1   # object proxy, don't know why this is installed
mccabe==0.6.1              # code complexity checker
pylint==2.2.2              # linter, only for development
six==1.12.0                # write python code that works in both python 2 and 3
wrapt==1.11.1              # object proxy, don't know why this is installed
